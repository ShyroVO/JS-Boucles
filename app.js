let x = 0;

while (x < 10) {
    console.log("x a la valeur " + x + " lors de sont passage n°" + (x + 1));

    x++;
}

for (let y = 0; y < 10; y++){
    console.log("y a la valeur " + y + " lors de sont passage n°" + (y + 1));
}